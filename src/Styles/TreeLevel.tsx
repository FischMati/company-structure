import styled from "styled-components";

const TreeLevel = styled.ul`
    padding-top: 20px; position: relative;
    
    transition: all 0.5s;
    -webkit-transition: all 0.5s;
    -moz-transition: all 0.5s;
`;

export default TreeLevel;
